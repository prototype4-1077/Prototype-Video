name: Render You Were Always Source New Voice

on:
  push:
    branches:
      - main
    paths:
      - build/you-were-always-source-new-voice/render.trigger

permissions:
  contents: write

jobs:
  render:
    permissions:
      contents: write
    uses: ./.github/workflows/render.yml
    with:
      slug: you-were-always-source-new-voice
    secrets: inherit

  record-status:
    if: ${{ always() }}
    needs:
      - render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Record caller status
        shell: bash
        run: |
          mkdir -p build/you-were-always-source-new-voice
          printf '%s\n' '${{ github.run_id }}' > build/you-were-always-source-new-voice/caller-run-id.txt
          printf '%s\n' '${{ needs.render.result }}' > build/you-were-always-source-new-voice/caller-conclusion.txt
          git config user.name render-bot
          git config user.email render@users.noreply.github.com
          git add build/you-were-always-source-new-voice/caller-run-id.txt build/you-were-always-source-new-voice/caller-conclusion.txt
          git commit -m 'render you-were-always-source-new-voice: record status' || exit 0
          for i in 1 2 3 4 5; do
            git push && exit 0
            git pull --rebase origin main || git rebase --abort || true
          done
          exit 1
