name: Render Reality Was Never Hidden

on:
  push:
    branches:
      - main
    paths:
      - build/trained-not-to-look/render.trigger

permissions:
  contents: write

jobs:
  render:
    permissions:
      contents: write
    uses: ./.github/workflows/render.yml
    with:
      slug: trained-not-to-look
    secrets: inherit

  record-status:
    if: ${{ always() }}
    needs:
      - render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Record caller status
        shell: bash
        run: |
          mkdir -p build/trained-not-to-look
          printf '%s\n' '${{ github.run_id }}' > build/trained-not-to-look/caller-run-id.txt
          printf '%s\n' '${{ needs.render.result }}' > build/trained-not-to-look/caller-conclusion.txt
          git config user.name render-bot
          git config user.email render@users.noreply.github.com
          git add build/trained-not-to-look/caller-run-id.txt build/trained-not-to-look/caller-conclusion.txt
          git commit -m 'render trained-not-to-look: record status' || exit 0
          for i in 1 2 3 4 5; do
            git push && exit 0
            git pull --rebase origin main || git rebase --abort || true
          done
          exit 1
