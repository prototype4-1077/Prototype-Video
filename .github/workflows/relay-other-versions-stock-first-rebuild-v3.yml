name: Relay Other Versions Stock First Rebuild V3

on:
  workflow_run:
    workflows: ["Render Other Versions Stock First Rebuild V3"]
    types: [completed]

permissions:
  actions: read

env:
  RELAY_URL: https://filebin.net/c4e95b27a6d842d99a10e7f3518b6c4d/delivery.json

jobs:
  relay:
    runs-on: ubuntu-latest
    steps:
      - name: Publish temporary authenticated delivery pointer
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_ID: ${{ github.event.workflow_run.id }}
          CONCLUSION: ${{ github.event.workflow_run.conclusion }}
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, os, pathlib, subprocess, time, urllib.request
          run_id = int(os.environ["RUN_ID"])
          conclusion = os.environ["CONCLUSION"]
          token = os.environ["GH_TOKEN"]
          payload = {
              "workflow": "Render Other Versions Stock First Rebuild V3",
              "run_id": run_id,
              "conclusion": conclusion,
          }
          if conclusion == "success":
              time.sleep(8)
              req = urllib.request.Request(
                  f"https://api.github.com/repos/jameswatson1077/tiktok-videos/actions/runs/{run_id}/artifacts",
                  headers={
                      "Authorization": f"Bearer {token}",
                      "Accept": "application/vnd.github+json",
                      "X-GitHub-Api-Version": "2022-11-28",
                      "User-Agent": "stock-first-delivery-relay-v3",
                  },
              )
              with urllib.request.urlopen(req, timeout=30) as response:
                  artifacts = json.load(response).get("artifacts") or []
              artifact = next(
                  (item for item in artifacts if item.get("name") == "other-versions-stock-first-rebuild-v3"),
                  artifacts[0] if artifacts else None,
              )
              if artifact is None:
                  raise SystemExit("successful V3 render has no artifact")
              artifact_id = int(artifact["id"])
              download = urllib.request.Request(
                  f"https://api.github.com/repos/jameswatson1077/tiktok-videos/actions/artifacts/{artifact_id}/zip",
                  headers={
                      "Authorization": f"Bearer {token}",
                      "Accept": "application/vnd.github+json",
                      "X-GitHub-Api-Version": "2022-11-28",
                      "User-Agent": "stock-first-delivery-relay-v3",
                  },
              )
              opener = urllib.request.build_opener(urllib.request.HTTPRedirectHandler())
              with opener.open(download, timeout=30) as response:
                  signed_url = response.geturl()
              payload.update({
                  "artifact_id": artifact_id,
                  "artifact_name": artifact.get("name"),
                  "artifact_size_bytes": artifact.get("size_in_bytes"),
                  "signed_download_url": signed_url,
              })
          else:
              result = subprocess.run(
                  ["gh", "run", "view", str(run_id), "--repo", "jameswatson1077/tiktok-videos", "--log-failed"],
                  capture_output=True, text=True,
              )
              text = (result.stdout or "") + "\n" + (result.stderr or "")
              payload["failure_excerpt"] = [
                  line for line in text.splitlines()
                  if "ERROR" in line or "FIX" in line or "failed" in line.lower() or "exact" in line.lower()
              ][-50:]
              payload["failure_log_tail"] = text[-16000:]
          pathlib.Path("/tmp/delivery-v3.json").write_text(
              json.dumps(payload, indent=2) + "\n", encoding="utf-8"
          )
          print(json.dumps(payload, indent=2))
          PY
          curl --fail --silent --show-error --retry 5 --retry-delay 2 \
            -X POST --data-binary @/tmp/delivery-v3.json "$RELAY_URL"
