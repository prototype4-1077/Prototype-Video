name: Render Can You Fly V3

on:
  push:
    paths:
      - "build/can-you-fly-v3/render.trigger"

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate exact visual plan
        run: python3 pipeline/visual_symbols.py validate build/can-you-fly-v3

  render:
    needs: validate
    uses: ./.github/workflows/render.yml
    with:
      slug: can-you-fly-v3
    secrets: inherit
